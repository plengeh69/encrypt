#### encrypt

- `apt update && apt upgrade`

- `apt install git`

- `git clone https://github.com/plengeh69/encrypt`

- `cd encrypt `

- `apt install python -y`

- `pip install emojify`

- `bash encrypt.sh`
